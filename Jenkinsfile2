pipeline {
  agent {
        docker { 
            image 'https://hub.docker.com/repository/docker/sergkharkin/speedtest'
        }
    }
  stages {
        stage("List repository") {
            steps {
                sh '''
                docker ps -a
                '''
            }
        }
    }
    post { 
        always { 
            cleanWs()
        }
    }
}
